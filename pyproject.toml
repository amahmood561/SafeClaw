[project]
name = "openclaw-local"
version = "0.1.0"
description = "A local OpenClaw-style terminal agent"
requires-python = ">=3.10"
dependencies = [
  "python-dotenv>=1.0.1",
  "requests>=2.32.3",
  "rich>=13.7.1",
  "typer>=0.12.5"
]

[project.scripts]
openclaw = "openclaw.cli:app"
